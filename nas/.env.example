# Copier en « .env » et compléter (pour le conteneur Caddy de ce dossier).

# Domaine qui pointe vers le NAS (DNS/DDNS vers ton IP publique).
GM_DOMAIN=gardemanger.tondomaine.fr

# E-mail pour les notifications du certificat Let's Encrypt.
GM_EMAIL=j.melchior87@gmail.com

# Nom du réseau Docker créé par le compose Supabase (cf « docker network ls »).
SUPABASE_NETWORK=supabase_default
