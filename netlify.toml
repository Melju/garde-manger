# Configuration de déploiement Netlify
[build]
  command = "npm run build"
  publish = "dist"

# Fallback SPA : toutes les routes servent index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
